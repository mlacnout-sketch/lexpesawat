name: zivpn_net_reset
description: A simple Shizuku-based Airplane Mode toggler.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shizuku_api: 1.2.1
  cupertino_icons: ^1.0.6
  http: ^1.2.0
  url_launcher: ^6.2.0
  provider: ^6.1.1 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true